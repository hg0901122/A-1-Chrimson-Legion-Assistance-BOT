<!DOCTYPE html>
<html lang="en">
<head><%- include('partials/head', { title: 'Not Found' }) %></head>
<body>
<div class="shell">
  <%- include('partials/nav') %>
  <div class="main">
    <%- include('partials/topbar', { pageTitle: '404', pageSub: 'Nothing on record here' }) %>
    <div class="content">
      <div class="panel">
        <div class="empty-state">
          <h3>Page not found</h3>
          <p>That route doesn't exist. <a href="/">Back to the overview</a>.</p>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html>
